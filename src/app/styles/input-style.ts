import { css } from '@emotion/react'

export const inputStyle = css`
  background-color: transparent;
  border: none;
  font-size: 24px;
  font-weight: 800;
  padding: 0;

  :focus {
    outline: none;
  }
`
